__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# Espace de travail du pilotage par sous-agents (scratch, ignoré par git)
.superpowers/

# Manifeste et secrets du propriétaire : jamais dans le dépôt.
# Ils vivent sur G:\retro\ côté VM.
emulators.toml
secrets.toml

# Gravé à la construction par outils/identite_build.py : sa valeur décrit UNE
# roue, pas un état du dépôt. Versionné, il ferait mentir toute construction
# qui ne le régénère pas.
retro/_identite.py
